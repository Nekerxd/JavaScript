// Escreva uma função que recebe 2 números e retorne o maior deles

function maior(num1, num2) {
    return num1 > num2 ? num1 : num2;
}

const maior2 = (x, y) => num1 > num2 ? num1 : num2;

console.log(maior(2, 8));
console.log(maior2(2, 8));